{
    "name": "koru-booking-root",
    "private": true,
    "scripts": {
        "deploy": "node scripts/deploy-cloudflare.js",
        "deploy:backend": "node scripts/deploy-vercel.js",
        "deploy:backoffice": "node scripts/deploy-cloudflare.js backoffice",
        "deploy:widget": "node scripts/deploy-cloudflare.js widget",
        "build:backoffice": "cd backoffice && npm install && npm run build",
        "build:widget": "cd widget && npm install && npm run build"
    },
    "devDependencies": {
        "rimraf": "^5.0.5"
    }
}